# df-remuneration-collector
Solução em Python para coletar dados de remuneração de servidores do DF no Portal da Transparência, armazenar em PostgreSQL normalizado, expor consulta via API paginada, permitir exportação em CSV/XLSX e monitorar execuções com Redis, agendamento e telemetria
